# Binaries
/bin/
/api
!/api/
/admin
!/admin/
# `go build ./...` drops a binary for the e2e seeder (web/spa/e2e/seed)
# in the repo root, same as /api and /admin above.
/seed
/routewerk
*.exe
*.exe~
*.dll
*.so
*.dylib

# Build
/tmp/
/dist/

# Local database backups (make backup-now)
/backups/

# Environment
.env
.env.local
.env.production

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Test
coverage.out
coverage.html

# Vendor (we use modules)
/vendor/
