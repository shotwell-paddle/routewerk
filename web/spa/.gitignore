node_modules/
.svelte-kit/
.env
.env.*
!.env.example
.DS_Store

# Build output is generated by `make spa-build` and never committed.
# Plain `go build ./...` doesn't need it (the embed is gated behind the
# spa_embed build tag — see web/spa/embed.go vs embed_stub.go).
build/
